# Shared dependencies for the evaluation harness.
# System-specific dependencies (OCR engines, cloud SDKs, LLM clients, vLLM)
# are documented separately per system under setup/ and docs/setup.md.

numpy
pyyaml
pillow
requests
tqdm

# Optional: fast edit distance backend for Flexible Character Accuracy
# (the FCA module falls back to a pure-Python implementation if absent).
rapidfuzz
